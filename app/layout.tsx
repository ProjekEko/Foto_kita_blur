import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Foto Kita blur.. ",
  description: "Web iseng buat ikutan tren aja guyss... Pencet mulai, ikutin lagunya, terus nikmatin hasilnya.",
  icons: {
    icon: "https://img.icons8.com/fluency/96/camera.png",
    apple: "https://img.icons8.com/fluency/96/camera.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
